module.exports = {
	async doAction(action, inputs) {},
};

/* ACTIONS :
//Send the result
const text = currentFunction.text;
const content =
    text.substring(0, text.indexOf("#")) +
    data.toString() +
    text.substring(text.lastIndexOf("#") + 1, text.length);
await interaction.reply({ content });
return;
    */
